#1. array args
#2. array1.concat(array2)
def using_concat(array1, array2)
    array1.concat(array2)
end

#1. Get array and element
#2. array.insert(4, element)
def using_insert(array, element)
    array.insert(4, element)
  end

#1. array arg
#2. array.uniq to get rid of duplicates
def using_uniq(arr)
  arr.uniq
end

#1. array arg
#2. array.flatten to return array of strings
def using_flatten(arr)
  arr.flatten
end

#1. Array arg and string arg
#2. array.delete(string)
def using_delete(arr, str)
  arr.delete(str)
end

#1. array arg and index arg
#2. arr.delete_at(index)
def using_delete_at(arr, index)
  arr.delete_at(index)
end
